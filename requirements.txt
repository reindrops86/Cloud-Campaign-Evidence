# Cloud Campaign Evidence Graph Dependencies
# Designed for standard Python 3.10+ environments with minimal dependencies.
requests>=2.28.0

# Optional: only needed for --source aws (live CloudTrail/IAM collection).
# Offline file-based investigation runs without it.
boto3>=1.34.0
